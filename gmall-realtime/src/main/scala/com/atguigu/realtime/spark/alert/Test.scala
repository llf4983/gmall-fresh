package com.atguigu.realtime.spark.alert

object Test {
  def main(args: Array[String]): Unit = {
    val set = new java.util.HashSet()
    import scala.collection.JavaConversions._
    for(ele <- set){
      println("***")
    }
  }
}
